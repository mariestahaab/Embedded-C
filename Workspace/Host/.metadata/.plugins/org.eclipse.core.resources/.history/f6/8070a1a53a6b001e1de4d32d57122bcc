/*
 * main.c
 *
 *  Created on: Oct 15, 2023
 *      Author: mariestahaab
 */

#include <stdio.h>
#include <stdint.h>

int main(void){

	uint16_t data = 0x0;

	printf("Data       = %4x\n", data);
	printf("Shifted val: %4x\n", data |= (0x1 << 3 | 0x1 << 2));
	printf("Shifted val: %4x\n", data |= (0x3 << 2));
	printf("Shifted val: %4x\n", data |= (0x11 << 2));

	// Bit Extraction:

	data = 0xac00;
	printf("Data       = %4x\n", data);

	//To extract bits 16 to 11 from 16-bit data:
	//	-shift the required bits to the LSB.
	//	-mask the required value and save it in a new variable.

	printf("Data       = %8x\n", data >> 10);

	return 0;
}
