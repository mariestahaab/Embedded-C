/*
 * structExercise_162.c
 *
 *  Created on: Oct 18, 2023
 *      Author: mariestahaab
 */

/* Question: Decode the given packet and print the values of different fields.
 *
 * 32-bit Packet Contents:
 *
 * 			|  ADDR_MODE  |  SHORT_ADDR  |  LONG_ADDR  |  SENSOR  |  BAT  |  PAYLOAD  |  STATUS  |  CRC  |
 * bits:   		   1			  2			    8			 3		  3		   12		   1		 2
 *
 */


#include <stdint.h>
#include <stdio.h>

typedef struct{
	uint8_t		crc			;
	uint8_t		status		;
	uint16_t	payload		;
	uint8_t		bat			;
	uint8_t		sensor		;
	uint8_t		longAddr	;
	uint8_t		shortAddr	;
	uint8_t		addrMode	;
} Packet_t;

void dispPacketValues(Packet_t *pPkt);
void getPacketValues(uint32_t *pVal);

int main(void){

	uint32_t testPacketValue = 0xFFFFABCD;

	Packet_t Packet;
	Packet_t *pPacket = &Packet;

	pPacket -> crc 			= (testPacketValue >> 0	) & (0x3	);
	pPacket -> status 		= (testPacketValue >> 2	) & (0x1	);
	pPacket -> payload 		= (testPacketValue >> 3	) & (0xFFF	);
	pPacket -> bat 			= (testPacketValue >> 15) & (0x7	);
	pPacket -> sensor 		= (testPacketValue >> 18) & (0x7	);
	pPacket -> longAddr 	= (testPacketValue >> 21) & (0xFF	);
	pPacket -> shortAddr 	= (testPacketValue >> 29) & (0x3	);
	pPacket -> addrMode 	= (testPacketValue >> 31) & (0x1	);

	dispPacketValues(pPacket);

	return 0;
}

void getPacketValues(uint32_t *pVal){
	printf("Enter a 32-bit HEX Value: ");
	scanf("%X", pVal);
}

void dispPacketValues(Packet_t *pPkt){
	printf("Values are:\n");
	printf("CRC\t\t: %X\n"			,pPkt -> crc		);
	printf("STATUS\t\t: %X\n"		,pPkt -> status		);
	printf("PAYLOAD\t\t: %X\n" 		,pPkt -> payload	);
	printf("BAT\t\t: %X\n" 			,pPkt -> bat		);
	printf("SENSOR\t\t: %X\n" 		,pPkt -> sensor		);
	printf("LONG_ADDR\t: %X\n"	 	,pPkt -> longAddr	);
	printf("SHORT_ADDR\t: %X\n" 	,pPkt -> shortAddr	);
	printf("ADDR_MODE\t: %X\n" 		,pPkt -> addrMode	);
}
